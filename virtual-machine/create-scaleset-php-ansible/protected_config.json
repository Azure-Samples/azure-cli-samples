{
  "commandToExecute": "yum update -y --exclude=WALinuxAgent && yum install -y epel-release && yum install -y ansible git iptables-services && git clone https://github.com/devigned/azure-cli-samples.git && cd azure-cli-samples/scaleset-php-ansible/php && ansible-playbook site.yml -i hosts --connection=local"
}
