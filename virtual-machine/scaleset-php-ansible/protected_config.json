{
  "commandToExecute": "yum update -y && yum install -y epel-release && yum install -y ansible git iptables-services && git clone https://github.com/devigned/az-scaleset-vm-ext.git && cd az-scaleset-vm-ext/php && ansible-playbook site.yml -i hosts --connection=local"
}
